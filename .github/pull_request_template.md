## What does this change?

<!-- A short description of the change and why it's needed. Link the issue it fixes, e.g. "Fixes #123". -->

## How was it tested?

<!-- Steps you took to verify it. For UI changes, add screenshots or a recording — light and dark if both are affected. -->

## Checklist

- [ ] Targets the `staging` branch
- [ ] `pnpm lint`, `pnpm format:check` and `pnpm typecheck` pass
- [ ] `pnpm build:mac` succeeds (for changes to the Mac app)
